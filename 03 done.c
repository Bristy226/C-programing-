#include<stdio.h>
#define pi 3.1416
int main()
{
    /*printf("1st operation \n");
    int R;
    float area; //cnostant value is already defined earlier 
	printf("enter the value of Radius :");
    scanf("%d",&R);
    area=pi*R*R;
	printf(" expected area of the circle : %.2f\n\n\n\n", area);
	
    printf("2nd operation \n"); 
    
    int x,y,summation;
    printf("give the first integer value x:");
    scanf("%d",&x);
    printf("give the second integer value y:");
    scanf("%d",&y);
    summation=x+y; //in the question the value of 'x' and 'y' were fixed
    printf("sum of the above two integers =%d\n\n\n\n ",summation );
    
    printf("3nd operation \n"); 
    
    int p=25,q=35,r=15;
    if (p>q && q>r)
    {
    printf ("the largest number is :p=%d\n\n\n",p);
    }
    else if (q>p && p>r)
    {
    printf(" the largest number is :q=%d\n\n\n",q);
    }
    else 
    printf ( " largest number is :r=%d\n\n\n\n",r);
    
    printf("4th operation \n"); 
    
    int a=11,b=17,c=13,d=12,e=5,sum=0;
    if (a%2!=0){
    sum=sum+a;
    }
    if (b%2!=0){
    sum=sum+b;
    }
    if (c%2!=0){
    sum=sum+c;
    }
    if (d%2!=0){
    sum=sum+d;
    }
    if(e%2!=0){
    sum=sum+e;
    printf("the total summation of odd numbers =%d",sum);
    }
    else {
    printf("out of if block");
    }
    */
   
    int sum=0,number[5]=(11,17,13,12,5);
    for (i=1; i<=5; i++);{
    number[i]= ;
    
    
    
    }
    
    if (number%2!=0)
    {
    printf("odd number detected and the sum of all odd values is: sum=sum+   ",) ;
    }
    else 
    print (" out of if block");
    /*
    int i;
    int value[100];
    for (i=0; i<=99 ; i++)
    {
        value[i]=120+i;
        printf("%d\n",value[i]);
        //printf("a");}*/
    
    
    
    
    return 0;
}